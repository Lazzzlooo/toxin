mixin input(type, placeholder, isButton)
    .input__container
        input(type= type class= "input" placeholder= placeholder)
        if isButton
            button.input__button
                span.input__button-text_hidden Submit
                span.material-icons arrow_forward
